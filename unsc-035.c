/*Return statements*/
#include <stdio.h>

int main (void)
{
    /*
    return 0; meaning the program runs in an expected way
    syntax: return; or return expression;
    for example:
        return;
        return 0; or return (0);
        return (a+b);
    Simple use in a function
    
    double cube (double x)
    {
        return (x * x * x);
    }
    
    */
    
    return 0;
}