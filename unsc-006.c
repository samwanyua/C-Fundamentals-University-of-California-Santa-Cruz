#include<stdio.h>
int main(void)
{   
    /* The following are keywords in C:
    auto (absolute)	break	case	char	
    const	continue	default	do
    double	else	enum	extern	float	
    for	goto (absolute)	if
    int	long	register(absolute)	return	
    short	signed	sizeof	static (absolute)
    struct	switch	typedef	union	unsigned	
    void	volatile	while
    */
    printf(" Hello world\n");
    return 0;
}    