/*Function Prototype*/

#include <stdio.h>

int main (void)
{
 /* 
 DECLARATIONS & PROTOTYPES
 Declaring soething first before it is defined is also called a prototype
 for example

 double cube (double);
 int main()
 {
    a = cube(3.5);
 }

This definition of prototype can be just with parameter type or with names
double cube (double x); is equivalent to double cube(double); //signature

special signature for example 
int printf(...);

 */
    return 0;
}